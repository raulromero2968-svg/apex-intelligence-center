/**
 * Agent Type Definitions
 * Core types for agent configuration and task results
 */

export type AgentConfig = {
  id: string;
  name: string;
};

export type TaskResult = {
  status: string;
};
