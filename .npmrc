# pnpm configuration for Vercel compatibility (2025 standard)
# Fixes "Module not found" errors with Next.js + pnpm symlinked structure
node-linker=hoisted
shamefully-hoist=true
public-hoist-pattern=*
